/* ============================================================
   Endereço do servidor do carwoo
   ------------------------------------------------------------
   Este é o ÚNICO arquivo que muda quando o sistema sai do seu
   computador para a internet.

   Deixe vazio ("") enquanto estiver testando na sua máquina:
   assim o sistema procura o servidor em http://localhost:3000.

   Depois de publicar o servidor (Render, Railway, etc.), cole
   aqui o endereço que o serviço mostrar. Exemplo:

     window.CARWOO_API = "https://carwoo-api.onrender.com";

   Regras:
     - com https:// no começo
     - SEM barra no final
     - SEM /api no final (o sistema acrescenta sozinho)
   ============================================================ */

window.CARWOO_API = "";
